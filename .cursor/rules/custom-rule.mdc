---
description:
globs:
alwaysApply: true
---

# Rules

<!-- - Use OpenAI Response API instead of completion api, here is the doc: https://platform.openai.com/docs/api-reference/responses -->
- use shadcn when needed, use command to install like: npx shadcn@latest add card
- interface Text use Enlish.
- Use Proxy Setting if network doesn't work: export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890
- Use npm to install
- Use ai sdk to deal LLM, https://ai-sdk.dev/docs
- Do not run npm run dev since i've run it.
- Check components/ui before npx a new shadcn components.